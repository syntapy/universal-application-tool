#!/bin/bash

export AWS_DEFAULT_REGION=us-west-2
REGION=us-west-2

aws --endpoint-url=http://localhost:4566 --profile localstack cloudformation deploy --region ${REGION} --stack-name local_s3 --template-file ./bin/localstack/local_s3.yaml
